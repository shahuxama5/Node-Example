const names = require('./names');
const utils = require('./utils');
const data = require('./apternative-flavor');
const flv = require('./mind-stone');

console.log(data);
utils.sayHi(names.u);
utils.sayHi(names.m);
